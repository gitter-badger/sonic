(function() {
  var listFactory;

  listFactory = function(item) {
    if (!item(isntanceof(Sonic.AbstractView))) {
      return null;
    }
  };

  Shadow.show(Shadow);

  console.log('hello');

}).call(this);
